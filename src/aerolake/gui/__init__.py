"""AeroLake web GUI (Streamlit) — a click-to-capture front-end.

Optional layer (install with ``uv sync --extra gui``). It is a thin front-end
over the same engine as the ``aerolake-capture`` CLI; it adds no capture logic.
"""
